{
    "name": "smart-planner-root",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm run dev --prefix frontend\" \"./venv/bin/uvicorn backend.main:app --port 8500 --reload\"",
        "install:all": "npm install --prefix frontend && ./venv/bin/pip install -r requirements.txt"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}